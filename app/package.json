{
  "name": "pongbot",
  "version": "1.0.0",
  "description": "Ping-pong facilitator and leaderboard for slack",
  "main": "index.js",
  "repository": "https://github.com/TravisHoover/pongbot",
  "author": "Travis Hoover",
  "license": "MIT",
  "dependencies": {
    "@slack/web-api": "^5.8.0",
    "dynamodb-doc": "^1.0.0"
  },
  "scripts": {
    "test": "mocha tests/unit/",
    "build": "sam build",
    "deploy": "sam build && sam deploy"
  },
  "devDependencies": {
    "chai": "^4.2.0",
    "mocha": "^6.1.4"
  }
}
